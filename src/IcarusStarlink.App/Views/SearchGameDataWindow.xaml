<Window x:Class="IcarusStarlink.App.Views.SearchGameDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Search game data" Height="560" Width="680"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ContentBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Searches every item in the game's own data — by name, AND by where a value is referenced inside other items' JSON (e.g. which recipes consume 'Wood', or which items grant a stat). Double-click a result to open its real file."
                   Style="{StaticResource PageBodyTextStyle}" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged"/>
            <TextBlock x:Name="SearchPlaceholder" Text="e.g. Stone_Pickaxe, BaseMaximumHealth, Wood…"
                       Style="{StaticResource PlaceholderTextStyle}"/>
        </Grid>

        <ListBox x:Name="ResultsListBox" Grid.Row="2" FontFamily="Consolas" FontSize="12"
                 MouseDoubleClick="ResultsListBox_MouseDoubleClick"/>

        <TextBlock x:Name="CountText" Grid.Row="3" Margin="0,6,0,0" FontSize="12" TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryForegroundBrush}"/>
    </Grid>
</Window>
