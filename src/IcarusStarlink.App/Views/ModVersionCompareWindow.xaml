<Window x:Class="IcarusStarlink.App.Views.ModVersionCompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="What the author changed" Height="620" Width="960" MinHeight="440" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ContentBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding ModName}" Style="{StaticResource PageHeaderStyle}"/>
        <TextBlock Grid.Row="1" Text="{Binding SummaryMessage}" Style="{StaticResource PageBodyTextStyle}"
                   TextWrapping="Wrap" MaxWidth="880" Margin="0,4,0,16"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" ItemsSource="{Binding ChangedFiles}" SelectedItem="{Binding SelectedFile}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4">
                            <TextBlock Text="{Binding RelativePath}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Summary}" FontSize="11" Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <DataGrid Grid.Column="2" ItemsSource="{Binding SelectedFile.Rows}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="140"/>
                    <DataGridTextColumn Header="Field" Binding="{Binding FieldName}" Width="130"/>
                    <DataGridTemplateColumn Header="Change" Width="100" CellTemplate="{StaticResource ChangeKindBadgeTemplate}"/>
                    <!-- Star-sized so both values stay visible at the default window size — the
                         whole point of this window is reading them side by side, and fixed widths
                         pushed the new value off the right edge (caught live). -->
                    <DataGridTextColumn Header="Old version" Binding="{Binding OldValueDisplay}" Width="*" MinWidth="90"/>
                    <DataGridTextColumn Header="New version" Binding="{Binding NewValueDisplay}" Width="*" MinWidth="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Row="3" Visibility="{Binding HasAssetDifferences, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,12,0,0">
            <TextBlock Text="Asset files the author changed (models, textures, icons)" Style="{StaticResource FieldLabelStyle}" Margin="0,0,0,4"/>
            <ListBox ItemsSource="{Binding AssetDifferences}" MaxHeight="110" FontSize="12" FontFamily="Consolas"/>
        </StackPanel>
    </Grid>
</Window>
