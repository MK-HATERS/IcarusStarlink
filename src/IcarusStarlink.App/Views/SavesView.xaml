<UserControl x:Class="IcarusStarlink.App.Views.SavesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Margin="32,32,32,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource PageHeaderStyle}"/>
        <TextBlock Grid.Row="1" Text="Edits Icarus player data. Every save and restore takes a full backup of the slot first, and nothing writes while the game is running — but a manual backup before experimenting is still the smart move."
                   Style="{StaticResource PageBodyTextStyle}" Margin="0,4,0,12" TextWrapping="Wrap" MaxWidth="720"/>

        <!-- Slot picker + backup/restore. The slot picker stays inline (every tab depends on it);
             Backup now/Restore/Open backups folder — real but occasional — collapse into one
             Expander next to it, matching Merge & Install's own "Advanced actions" pattern, so they
             don't compete with the picker for attention on every visit. -->
        <DockPanel Grid.Row="2" Margin="0,0,0,12" LastChildFill="True">
            <ComboBox DockPanel.Dock="Left" ItemsSource="{Binding Slots}" SelectedItem="{Binding SelectedSlot}" MinWidth="240" Margin="0,0,8,0"
                      ToolTip="Steam player slots found under the game's own PlayerData folder">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Display}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Expander IsExpanded="False" HorizontalAlignment="Left" AutomationProperties.Name="Backup and restore">
                <Expander.Header>
                    <DockPanel LastChildFill="False">
                        <TextBlock DockPanel.Dock="Left" Text="Backup &amp; restore" Style="{StaticResource ExpanderHeaderTitleStyle}"/>
                        <TextBlock DockPanel.Dock="Left" Text="{Binding LastBackupDisplay}" Style="{StaticResource ExpanderHeaderStateStyle}"/>
                    </DockPanel>
                </Expander.Header>
                <WrapPanel>
                    <Button Command="{Binding BackupNowCommand}" AutomationProperties.Name="Backup now"
                            ToolTip="Zips the whole slot into this app's own save_backups cache — never inside the game's folders">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconDownload}" Stroke="{DynamicResource ForegroundBrush}" StrokeThickness="1.8"
                                  StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0"/>
                            <TextBlock Text="Backup now" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <ComboBox ItemsSource="{Binding Backups}" SelectedItem="{Binding SelectedBackup}" MinWidth="230" Margin="8,0,0,0"
                              ToolTip="Backups of this slot, newest first">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Display}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Content="Restore" Command="{Binding RestoreBackupCommand}" Margin="8,0,0,0"
                            ToolTip="Replaces the slot with the selected backup — writes a pre_restore safety zip of the current state first, so this is itself undoable"/>
                    <Button Content="Open backups folder" Command="{Binding OpenBackupsFolderCommand}" Margin="8,0,0,0"/>
                </WrapPanel>
            </Expander>
        </DockPanel>

        <TabControl Grid.Row="3" SelectedIndex="{Binding SelectedTabIndex}">
            <!-- Overview: character cards + account snapshot, per Icarus Workshop's own save
                 editor layout ("character cards plus account snapshot ... Click a card to edit"). -->
            <TabItem Header="Overview">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding Characters}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.EditCharacterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}" Padding="0" Margin="0,0,12,12"
                                            AutomationProperties.Name="{Binding Name}"
                                            ToolTip="Click to edit this character">
                                        <Border Width="240" Background="{DynamicResource CardBackgroundBrush}"
                                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="16,12">
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="15" TextTrimming="CharacterEllipsis"/>
                                                    <Border Margin="8,0,0,0" CornerRadius="8" Padding="6,1" VerticalAlignment="Center"
                                                            Background="{DynamicResource DangerSoftBrush}"
                                                            Visibility="{Binding IsDead, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="DEAD" FontSize="10" FontFamily="Consolas" Foreground="{DynamicResource DangerBrush}"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="{DynamicResource SecondaryForegroundBrush}">
                                                    <Run Text="Slot "/><Run Text="{Binding ChrSlot, Mode=OneWay}"/><Run Text=" · "/><Run Text="{Binding LocationDisplay, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock Margin="0,8,0,0" FontFamily="Consolas" FontSize="13">
                                                    <Run Text="XP " Foreground="{DynamicResource FaintForegroundBrush}"/><Run Text="{Binding XpDisplay, Mode=OneWay}" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Account snapshot -->
                        <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1" CornerRadius="6" Padding="16,12" HorizontalAlignment="Left" MinWidth="360" Margin="0,4,0,0">
                            <StackPanel>
                                <TextBlock Text="Account" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding Currencies}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="2"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel Margin="0,2,18,2" LastChildFill="False">
                                                <TextBlock DockPanel.Dock="Left" Text="{Binding Label}" Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="12.5" VerticalAlignment="Center"/>
                                                <TextBlock DockPanel.Dock="Right" Text="{Binding CountText}" FontFamily="Consolas" FontSize="12.5" VerticalAlignment="Center" Margin="14,0,0,0"/>
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Characters: edit the selected character + profile currencies (spec: "Profile
                 currencies on the same tab"). -->
            <TabItem Header="Characters">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ListBox Grid.Row="0" ItemsSource="{Binding Characters}" SelectedItem="{Binding SelectedCharacter}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <TextBlock Text=" *" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"
                                                   Visibility="{Binding IsDirty, Converter={StaticResource BoolToVisibilityConverter}}"
                                                   ToolTip="Unsaved changes"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Act on whichever character is selected above. Duplicate is instant, no
                             confirm (matches ExmodEditorViewModel's own Duplicate) — it only adds
                             data. Delete removes the character's entire progress and confirms first,
                             unlike every other destructive edit on this page (those stay staged in
                             memory until Save's own confirm covers them; a whole character is too
                             big a loss for that). -->
                        <WrapPanel Grid.Row="1" Margin="0,8,0,0">
                            <Button Command="{Binding DuplicateCharacterCommand}" CommandParameter="{Binding SelectedCharacter}"
                                    AutomationProperties.Name="Duplicate character"
                                    ToolTip="Clones the selected character under a fresh character slot — talents/flags/cosmetics all come along, the name is suffixed '(Copy)'">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconCopy}" Stroke="{DynamicResource ForegroundBrush}" StrokeThickness="1.6"
                                          StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock Text="Duplicate" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding DeleteCharacterCommand}" CommandParameter="{Binding SelectedCharacter}"
                                    Margin="8,0,0,0" AutomationProperties.Name="Delete character"
                                    ToolTip="Permanently removes the selected character's entire progress — confirms first">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconTrash}" Stroke="{DynamicResource DangerBrush}" StrokeThickness="1.6"
                                          StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Width="11" Height="11" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock Text="Delete" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                    </Grid>

                    <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="560" HorizontalAlignment="Left"
                                    Visibility="{Binding SelectedCharacter, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock Text="Character name" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding SelectedCharacter.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" MaxWidth="280" HorizontalAlignment="Left" MinWidth="280"/>

                            <TextBlock Text="XP" Style="{StaticResource FieldLabelStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,2">
                                <TextBox Text="{Binding SelectedCharacter.XpText, UpdateSourceTrigger=PropertyChanged}" MinWidth="160" FontFamily="Consolas"/>
                                <TextBlock Text="{Binding SelectedCharacter.XpDisplay}" VerticalAlignment="Center" Margin="10,0,0,0"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}" FontFamily="Consolas" FontSize="12"/>
                            </StackPanel>
                            <TextBlock Text="Raw experience points — the game derives level from this (its own level curve lives in a compiled asset, so a level slider would be a guess; the number is the honest control)."
                                       Foreground="{DynamicResource FaintForegroundBrush}" FontSize="11.5" TextWrapping="Wrap" Margin="0,0,0,12"/>

                            <TextBlock Margin="0,0,0,14" FontSize="12.5" Foreground="{DynamicResource SecondaryForegroundBrush}">
                                <Run Text="Slot "/><Run Text="{Binding SelectedCharacter.ChrSlot, Mode=OneWay}"/><Run Text=" · Location: "/><Run Text="{Binding SelectedCharacter.Location, Mode=OneWay}"/>
                            </TextBlock>

                            <TextBlock Text="Profile currencies" Style="{StaticResource FieldLabelStyle}" Margin="0,4,0,4"/>
                            <ItemsControl ItemsSource="{Binding Currencies}" MaxWidth="560">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel Margin="0,3,16,3">
                                            <TextBlock DockPanel.Dock="Left" Text="{Binding Label}" Width="130" VerticalAlignment="Center" FontSize="12.5"/>
                                            <TextBox Text="{Binding CountText, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas" FontSize="12.5"/>
                                        </DockPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <!-- Talents: character talents searchable with rank editing; workshop research below.
                 Every talent the game defines is available via "Show unlearned" — turning a rank
                 up grants it (rank 0 is the format's own "doesn't have it"). -->
            <TabItem Header="Talents">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="150"/>
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" Margin="0,0,0,8">
                        <Grid Width="240" VerticalAlignment="Center">
                            <TextBox Text="{Binding TalentSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Search talents…" Style="{StaticResource PlaceholderTextStyle}"
                                       Visibility="{Binding TalentSearchText, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}"/>
                        </Grid>
                        <CheckBox Style="{StaticResource ToggleSwitchStyle}" Content="Show unlearned" IsChecked="{Binding ShowUnlearnedTalents}"
                                  Margin="12,0,0,0" VerticalAlignment="Center"
                                  ToolTip="Lists every talent the game defines, not just this character's — set a rank above 0 to grant one"/>
                    </WrapPanel>

                    <ListBox Grid.Row="1" ItemsSource="{Binding FilteredTalents}" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="2" ToolTip="{Binding Description}">
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="−" Padding="8,0" Command="{Binding RankDownCommand}" AutomationProperties.Name="Rank down"/>
                                        <!-- Shown as rank/max — the max comes from the talent's own Rewards tiers
                                             in the game data, and + stops there: a rank beyond it doesn't exist. -->
                                        <TextBlock Text="{Binding RankDisplay}" MinWidth="40" TextAlignment="Center" VerticalAlignment="Center" FontFamily="Consolas"/>
                                        <Button Content="+" Padding="8,0" Command="{Binding RankUpCommand}" IsEnabled="{Binding CanRankUp}" AutomationProperties.Name="Rank up"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock FontSize="12.5">
                                            <Run Text="{Binding DisplayName, Mode=OneWay}" FontWeight="SemiBold"/><Run Text="  "/><Run Text="{Binding RowName, Mode=OneWay}" Foreground="{DynamicResource FaintForegroundBrush}" FontSize="10.5"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Tree}" FontSize="11" Foreground="{DynamicResource FaintForegroundBrush}"/>
                                    </StackPanel>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <TextBlock Grid.Row="2" Text="Workshop research (account-wide)" Style="{StaticResource FieldLabelStyle}" Margin="0,10,0,4"/>
                    <ListBox Grid.Row="3" ItemsSource="{Binding WorkshopTalents}" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="2" ToolTip="{Binding Description}">
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="−" Padding="8,0" Command="{Binding RankDownCommand}" AutomationProperties.Name="Rank down"/>
                                        <TextBlock Text="{Binding RankDisplay}" MinWidth="40" TextAlignment="Center" VerticalAlignment="Center" FontFamily="Consolas"/>
                                        <Button Content="+" Padding="8,0" Command="{Binding RankUpCommand}" IsEnabled="{Binding CanRankUp}" AutomationProperties.Name="Rank up"/>
                                    </StackPanel>
                                    <TextBlock FontSize="12.5" VerticalAlignment="Center">
                                        <Run Text="{Binding DisplayName, Mode=OneWay}"/><Run Text="  "/><Run Text="{Binding RowName, Mode=OneWay}" Foreground="{DynamicResource FaintForegroundBrush}" FontSize="10.5"/>
                                    </TextBlock>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>

            <!-- Flags: searchable chips, character flags + account flags — names straight from the
                 game's own D_CharacterFlags/D_AccountFlags tables (the int in the save is the row
                 index), so they stay correct across game updates. -->
            <TabItem Header="Flags">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <StackPanel>
                        <Grid Width="240" HorizontalAlignment="Left" Margin="0,0,0,10">
                            <TextBox Text="{Binding FlagSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Search flags…" Style="{StaticResource PlaceholderTextStyle}"
                                       Visibility="{Binding FlagSearchText, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}"/>
                        </Grid>

                        <TextBlock Text="Character flags" Style="{StaticResource FieldLabelStyle}" Margin="0,0,0,6"/>
                        <ItemsControl ItemsSource="{Binding FilteredCharacterFlags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource ToggleSwitchStyle}" Content="{Binding Name}" IsChecked="{Binding IsUnlocked}"
                                              Margin="0,2,16,2" ToolTip="{Binding Id}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="Account flags" Style="{StaticResource FieldLabelStyle}" Margin="0,14,0,6"/>
                        <ItemsControl ItemsSource="{Binding FilteredAccountFlags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource ToggleSwitchStyle}" Content="{Binding Name}" IsChecked="{Binding IsUnlocked}"
                                              Margin="0,2,16,2" ToolTip="{Binding Id}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- The game keeps a THIRD unlock store beside the JSON ones: a binary
                             flags_<SteamID>.dat of account-wide character-flag unlocks (mission
                             rewards, talent-granted recipes). Shown only when the game itself has
                             created the file for this slot. -->
                        <StackPanel Visibility="{Binding HasBinaryFlags, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Account-wide unlocks (binary flags file)" Style="{StaticResource FieldLabelStyle}" Margin="0,14,0,2"/>
                            <TextBlock Text="From the slot's own flags file, which the game keeps separately from the JSON flags above — mission and talent-granted unlocks live here."
                                       Foreground="{DynamicResource FaintForegroundBrush}" FontSize="11.5" TextWrapping="Wrap" Margin="0,0,0,6"/>
                            <ItemsControl ItemsSource="{Binding FilteredBinaryFlags}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Style="{StaticResource ToggleSwitchStyle}" Content="{Binding Name}" IsChecked="{Binding IsUnlocked}"
                                                  Margin="0,2,16,2" ToolTip="{Binding Id}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Bestiary: encounter points per creature, from the account-wide BestiaryData.json.
                 Points required to fully unlock a creature's field-guide entry come from the game's
                 own D_BestiaryData table (TotalPointsRequired) — Set max fills that in directly. -->
            <TabItem Header="Bestiary">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Width="240" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <TextBox Text="{Binding BestiarySearchText, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Search creatures…" Style="{StaticResource PlaceholderTextStyle}"
                                   Visibility="{Binding BestiarySearchText, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}"/>
                    </Grid>

                    <ListBox Grid.Row="1" ItemsSource="{Binding FilteredBestiaryEntries}" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="2">
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBox Text="{Binding PointsText, UpdateSourceTrigger=PropertyChanged}" Width="70" FontFamily="Consolas" TextAlignment="Right"/>
                                        <TextBlock Text="{Binding PointsRequired, StringFormat=' / {0}'}" VerticalAlignment="Center" Margin="4,0,0,0"
                                                   Foreground="{DynamicResource SecondaryForegroundBrush}" FontFamily="Consolas" FontSize="12"/>
                                        <Button Content="Set max" Command="{Binding SetMaxCommand}" Padding="8,2" Margin="8,0,0,0"
                                                ToolTip="Fills in this creature's own TotalPointsRequired — fully unlocks its field-guide entry"/>
                                    </StackPanel>
                                    <!-- Resolved lazily off the base game's own extracted pak content (see
                                         IBaseGameIconDecoder) — collapsed entirely until (if ever) it
                                         resolves, so a row never shows an empty placeholder box. -->
                                    <Border DockPanel.Dock="Left" Width="28" Height="28" Margin="0,0,8,0"
                                            Background="{DynamicResource CardRaisedBackgroundBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4"
                                            Visibility="{Binding Icon, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Image Source="{Binding Icon}" Stretch="Uniform" Margin="2"/>
                                    </Border>
                                    <StackPanel>
                                        <TextBlock FontSize="12.5">
                                            <Run Text="{Binding DisplayName, Mode=OneWay}" FontWeight="SemiBold"/><Run Text="  "/><Run Text="{Binding RowName, Mode=OneWay}" Foreground="{DynamicResource FaintForegroundBrush}" FontSize="10.5"/>
                                        </TextBlock>
                                        <TextBlock Text="Boss" FontSize="11" Foreground="{DynamicResource AccentBrush}"
                                                   Visibility="{Binding IsBoss, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </StackPanel>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>

            <!-- Accolades: completion chips, from the account-wide Accolades.json. Only
                 CompletedAccolades is editable here — the raw progress counters that make an
                 accolade eligible (PlayerTrackers/PlayerTaskListTrackers) are preserved untouched. -->
            <TabItem Header="Accolades">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <StackPanel>
                        <Grid Width="240" HorizontalAlignment="Left" Margin="0,0,0,10">
                            <TextBox Text="{Binding AccoladeSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Search accolades…" Style="{StaticResource PlaceholderTextStyle}"
                                       Visibility="{Binding AccoladeSearchText, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding FilteredAccolades}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource ToggleSwitchStyle}" Content="{Binding DisplayName}" IsChecked="{Binding IsCompleted}"
                                              Margin="0,2,16,2" ToolTip="{Binding Description}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Cosmetics: raw Customization_* hashes for the selected character — no name mapping
                 exists (confirmed by direct testing, see SaveCosmeticFieldViewModel), so this is a
                 raw editor, not a picker. Still useful for copying an exact look between your own
                 characters. -->
            <TabItem Header="Cosmetics">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <StackPanel MaxWidth="480" HorizontalAlignment="Left"
                                Visibility="{Binding SelectedCharacter, Converter={StaticResource NullToVisibilityConverter}}">
                        <TextBlock Text="These are raw hash values with no known way to map them to option names — useful mainly for copying an exact look from one of your own characters to another (read the value here, paste it there)."
                                   Foreground="{DynamicResource FaintForegroundBrush}" FontSize="11.5" TextWrapping="Wrap" Margin="0,0,0,10"/>

                        <CheckBox Style="{StaticResource ToggleSwitchStyle}" Content="Male" IsChecked="{Binding SelectedCharacter.IsMale}" Margin="0,0,0,12"/>

                        <ItemsControl ItemsSource="{Binding SelectedCharacter.Cosmetics}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="0,3,0,3">
                                        <TextBlock DockPanel.Dock="Left" Text="{Binding Key}" Width="220" VerticalAlignment="Center" FontSize="12.5"/>
                                        <TextBox Text="{Binding ValueText, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Items: the account-wide MetaInventory bank — view + delete only (see
                 SaveInventoryItemViewModel's own doc comment for why per-field editing isn't
                 offered). Real display names resolved through D_ItemsStatic's own Itemable trait
                 reference into D_Itemable, since D_ItemsStatic rows carry no name of their own. -->
            <TabItem Header="Items">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Width="240" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <TextBox Text="{Binding ItemSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Search items…" Style="{StaticResource PlaceholderTextStyle}"
                                   Visibility="{Binding ItemSearchText, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}"/>
                    </Grid>

                    <ListBox Grid.Row="1" ItemsSource="{Binding FilteredMetaInventoryItems}" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="2">
                                    <Button DockPanel.Dock="Right" Content="Delete" Padding="8,2"
                                            Command="{Binding DataContext.DeleteMetaInventoryItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Removes this item from the meta bank — nothing else in the bank is touched"/>
                                    <!-- Resolved lazily off the base game's own extracted pak content (see
                                         IBaseGameIconDecoder) — collapsed entirely until (if ever) it
                                         resolves, so a row never shows an empty placeholder box. -->
                                    <Border DockPanel.Dock="Left" Width="28" Height="28" Margin="0,0,8,0"
                                            Background="{DynamicResource CardRaisedBackgroundBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4"
                                            Visibility="{Binding Icon, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Image Source="{Binding Icon}" Stretch="Uniform" Margin="2"/>
                                    </Border>
                                    <TextBlock VerticalAlignment="Center" FontSize="12.5">
                                        <Run Text="{Binding DisplayName, Mode=OneWay}" FontWeight="SemiBold"/><Run Text="  "/><Run Text="{Binding RowName, Mode=OneWay}" Foreground="{DynamicResource FaintForegroundBrush}" FontSize="10.5"/><Run Text="  "/><Run Text="{Binding WeightDisplay, Mode=OneWay}" Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="11"/><Run Text="  "/><Run Text="{Binding MaxStackDisplay, Mode=OneWay}" Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="11"/>
                                    </TextBlock>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>

            <!-- Mounts: account-wide, Mounts.json — a separate file from Profile.json. Name/Level/
                 Type are plain, safely editable fields; each mount's real stats/stomach/saddle state
                 lives in RecorderBlob, a raw Unreal binary-serialized blob (not JSON), which this
                 editor never touches. Type is a picker over D_Mounts's own real row names, since
                 that table carries no display-name field of its own. -->
            <TabItem Header="Mounts">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Width="240" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <TextBox Text="{Binding MountSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Search mounts…" Style="{StaticResource PlaceholderTextStyle}"
                                   Visibility="{Binding MountSearchText, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}"/>
                    </Grid>

                    <ListBox Grid.Row="1" ItemsSource="{Binding FilteredMounts}" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="2,4,2,4">
                                    <Button DockPanel.Dock="Right" Content="Delete" Padding="8,2"
                                            Command="{Binding DataContext.DeleteMountCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Removes this mount — nothing else in the list is touched"/>

                                    <!-- Resolved lazily off the base game's own extracted pak content (see
                                         IBaseGameIconDecoder) — collapsed entirely until (if ever) it
                                         resolves, so a row never shows an empty placeholder box. -->
                                    <Border DockPanel.Dock="Left" Width="28" Height="28" Margin="0,0,10,0"
                                            Background="{DynamicResource CardRaisedBackgroundBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4"
                                            Visibility="{Binding Icon, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Image Source="{Binding Icon}" Stretch="Uniform" Margin="2"/>
                                    </Border>

                                    <TextBox DockPanel.Dock="Left" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                             Width="150" Margin="0,0,10,0" ToolTip="This mount's name"/>

                                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" Margin="0,0,10,0" ToolTip="This mount's level">
                                        <TextBlock Text="Lv" VerticalAlignment="Center" Foreground="{DynamicResource FaintForegroundBrush}" Margin="0,0,4,0"/>
                                        <TextBox Text="{Binding LevelText, UpdateSourceTrigger=PropertyChanged}" Width="50" FontFamily="Consolas" TextAlignment="Right"/>
                                    </StackPanel>

                                    <ComboBox ItemsSource="{Binding AvailableTypeRowNames}" SelectedItem="{Binding TypeRowName}"
                                              Width="220" ToolTip="This mount's species — a real D_Mounts row name; D_Mounts carries no display name of its own, so this is a light word-split of the row name">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Converter={StaticResource MountTypeDisplayConverter}}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Save bar sits under the tabs, always visible -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,4,4">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,12,0" TextWrapping="Wrap" MaxWidth="520"
                       Foreground="{DynamicResource SecondaryForegroundBrush}" FontSize="12"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
            <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveChangesCommand}"
                    IsEnabled="{Binding HasUnsavedChanges}" AutomationProperties.Name="Save player data"
                    ToolTip="Writes your edits into the save — a full backup of the slot is taken automatically first. Refused while Icarus is running."
                    ToolTipService.ShowOnDisabled="True">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{StaticResource IconCheck}" Stroke="{DynamicResource AccentForegroundBrush}" StrokeThickness="1.8"
                          StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0"/>
                    <TextBlock Text="Save player data" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
