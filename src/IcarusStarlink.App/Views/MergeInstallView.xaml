<UserControl x:Class="IcarusStarlink.App.Views.MergeInstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel Style="{StaticResource PlaceholderPageStyle}">
        <TextBlock Text="{Binding Title}" Style="{StaticResource PageHeaderStyle}"/>
        <TextBlock Text="{Binding PlaceholderMessage}" Style="{StaticResource PageBodyTextStyle}"/>
    </StackPanel>
</UserControl>
