<Window x:Class="IcarusStarlink.App.Views.ValidationIssueReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Mod validation" Height="480" Width="980"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ContentBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="HeaderText" Style="{StaticResource PageHeaderStyle}" FontSize="18" TextWrapping="Wrap" Margin="0,0,0,4"/>
            <TextBlock TextWrapping="Wrap" FontSize="11.5" Foreground="{DynamicResource SecondaryForegroundBrush}">
                None of these stop the merge or the pak build from succeeding — Icarus's own importer just silently ignores a field it doesn't recognize, and an unresolved reference just quietly does nothing. Fix these in the EXMOD editor, then check again.
            </TextBlock>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="FindingsGrid" IsReadOnly="True" AutoGenerateColumns="False" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="80"/>
                <DataGridTextColumn Header="Mod" Binding="{Binding ModName}" Width="150"/>
                <DataGridTextColumn Header="File" Binding="{Binding File}" Width="190"/>
                <DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="130"/>
                <DataGridTextColumn Header="Field" Binding="{Binding FieldPath}" Width="110"/>
                <DataGridTextColumn Header="Why" Binding="{Binding Reason}" Width="*" MinWidth="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Close" Click="Close_Click" HorizontalAlignment="Right" Margin="0,12,0,0"/>
    </Grid>
</Window>
