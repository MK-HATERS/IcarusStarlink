<Window x:Class="IcarusStarlink.App.Views.RenameModDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Rename mod" SizeToContent="Height" Width="380" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ContentBackgroundBrush}">

    <StackPanel Margin="20">
        <TextBlock Text="Changes how this mod displays in your Library only — its real folder, file name, and mod content are never touched."
                   Style="{StaticResource PageBodyTextStyle}" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <TextBlock Text="Display name" Style="{StaticResource FieldLabelStyle}"/>
        <TextBox x:Name="NameBox" Margin="0,4,0,12"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Reset to default" Click="Reset_Click" Margin="0,0,8,0"
                    ToolTip="Clears the override — goes back to the mod's own declared name"/>
            <Button Content="Cancel" Click="Cancel_Click" Margin="0,0,8,0"/>
            <Button Content="Save" Click="Save_Click" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </StackPanel>
</Window>
