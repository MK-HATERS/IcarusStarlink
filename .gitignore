# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
out/

# App runtime data (created next to the exe when running, already under bin/ above).
# Not repeated as bare folder-name rules here: those matched by name anywhere in the tree,
# which silently hid src/IcarusStarlink.*/Profiles/ (real source, not runtime data) from git.
/settings.json

# Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.user
*.suo

# Claude Code local tooling state
.claude/

# NuGet
*.nupkg
packages/

# OS
Thumbs.db
.DS_Store
